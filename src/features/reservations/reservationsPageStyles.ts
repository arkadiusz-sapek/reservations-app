import styled from '@emotion/styled';

export const ReservationsWrapper = styled.main`
    display: flex;
    flex-direction: row;
    justify-content: space-around;
    padding: 2rem;
    height: 100vh;
    width: 100vw;
    box-sizing: border-box;
`;
